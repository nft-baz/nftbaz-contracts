// SPDX-License-Identifier: UNLICENSED
pragma solidity 0.8.24;

/// @notice Smallest non-empty contract — verifies the Foundry toolchain
///         + remappings + solc 0.8.24 are wired up correctly.
contract Sanity {
    uint256 public constant VERSION = 1;
}
